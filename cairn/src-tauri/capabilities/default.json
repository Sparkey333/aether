{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the sidebar: read the clipboard text, open gateways. Window + event basics come from core:default.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "clipboard-manager:allow-read-text",
    "opener:default"
  ]
}
