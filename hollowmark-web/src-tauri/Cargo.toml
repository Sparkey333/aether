[package]
name = "hollowmark"
version = "0.1.0"
description = "HOLLOWMARK greybox slice — desktop shell"
authors = ["HOLLOWMARK"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
strip = true
lto = true
