import CloneStudio from "@/components/CloneStudio";

export const metadata = {
  title: "Clone Studio — Aether",
  description:
    "Capture a turnaround and motion clips from the Mac camera or an iPhone via Continuity Camera, and build your AI clone fighter.",
};

export default function Page() {
  return <CloneStudio />;
}
