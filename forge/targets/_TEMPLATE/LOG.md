# Log — _TEMPLATE

> Step 3 (running). The honest record of what actually happened. Date-stamp
> entries. Capture the moments the agent went sideways AND how you corrected it —
> those corrections are the raw material the masterclass refines into presets.

## YYYY-MM-DD
- What I set the agent on:
- Where it nailed it:
- Where it drifted / failed:
- The correction that fixed it (prompt tweak, context add, guardrail):
- Carry-forward → which preset/prompt should absorb this lesson?
