# Plan — _TEMPLATE

> Step 2. Turn recon into an ordered build the agent can execute with minimal
> ambiguity. Each slice should end in something runnable and checkable.

## The setup you'll hand the agent
- CLAUDE.md / context used (which preset?):
- Provider + model:
- Agents / subagents in play:
- Allowed tools / permissions posture:

## Build slices (smallest-runnable-first)
1. [ ] Spark: <the minimal slice that runs> — checkpoint: <how you'll know it works>
2. [ ] …
3. [ ] …

## Guardrails for this build
- Don't touch: <files/areas off-limits>
- Verify after each slice with: <command/test>
- Stop-and-ask triggers: <ambiguities that should pause the agent>
