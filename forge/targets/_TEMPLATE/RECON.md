# Recon — _TEMPLATE

> Step 1 of the loop. Understand the target before you set the agent loose.
> Goal: a map accurate enough that the build phase is mostly mechanical.

## What is it?
- Purpose, in one sentence:
- Who uses it / what problem it solves:

## Shape of the thing
- Languages / frameworks:
- Approx size (LOC, # files, # services):
- Build & run command(s):
- Tests? How are they run?
- External services / APIs / secrets it needs:

## The skeleton (the parts that matter)
- Entry points:
- Core modules / "where the magic is":
- Data model / schemas:
- The 20% of files that carry 80% of the behavior:

## Hard parts (where a clone usually fails)
- Hidden state, config, env, codegen:
- Anything non-obvious (build flags, native deps, licensing):

## Reproduction plan seed
- Smallest runnable slice to clone FIRST (the "spark"):
- What "done" looks like for this rung:
